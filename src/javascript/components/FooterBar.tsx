import React from 'react';

const FooterBar: React.FC = () => {
  return (
    <div className="footer">
      <p>PDF to Markdown — Convert your PDF files to Markdown format</p>
    </div>
  );
};

export default FooterBar; 